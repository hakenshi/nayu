# X11 (Implementer Notes)

Minimal v0 scope:

- Set root window background pixmap.
- Publish standard root properties:
  - `_XROOTPMAP_ID`
  - `ESETROOT_PMAP_ID`

## Persistence

X11 wallpaper setters usually keep the pixmap alive via root properties.
Ensure the background persists after `nayu set` exits.
