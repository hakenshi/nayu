# nayu

`nayu` is a wallpaper engine for Wayland and X11.

This repository is documented primarily for implementers (see `docs/`).

- Start here: `docs/vision.md`
- IPC contract: `docs/ipc.md`

Developer shortcuts (optional): install `just` and run `just`.
